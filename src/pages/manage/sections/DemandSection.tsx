import { ManagePlaceholderSection } from "./ManagePlaceholderSection";

export default function DemandSection() {
  return (
    <ManagePlaceholderSection
      id="demand"
      title="Demand"
      description="Demand signals loaded lazily."
    />
  );
}
